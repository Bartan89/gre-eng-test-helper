import { StoreState } from "../types"

export const slctShowDictionary = (state: StoreState) => state.uistate.showDictionary


export const slctEmojisFlying = (state: StoreState) => state.uistate.emojiFlying
